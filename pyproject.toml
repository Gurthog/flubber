[project]
name = "flubber"
version = "0.1.0"
description = "Personal site for fun"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
    "gunicorn>=26.0.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "whitenoise>=6.12.0",
]
